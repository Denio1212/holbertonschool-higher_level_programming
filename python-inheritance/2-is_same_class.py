#!/usr/bin/python3
"""
Returns true if object is of same class
"""


def is_same_class(obj, a_class):
    """
    Is instance will be used
    """
    if isinstance(obj, a_class) is True:
        return True
    else:
        return False